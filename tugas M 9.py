import cprint
import termcolor
def draw_cake():
    for i in range(10):
        for j in range(10):
            if (i + j) % 2 == 0:
                cprint.cprint(f"{i}{j}", "red", "on_white")
            else:
                cprint.cprint(f"{i}{j}", "green", "on_white")
        for i in range(10):
            cprint.cprint(f"{i}", "yellow", "on_white")
def draw_cake():
    for i in range(10):
        for j in range(10):
            if (i + j) % 2 == 0:
                cprint.cprint(f"{i}{j}", "red", "on_white")
            else:
                cprint.cprint(f"{i}{j}", "green", "on_white")
        for i in range(10):
            cprint.cprint(f"{i}", "yellow", "on_white")
def draw_cake():
    for i in range(10):
        for j in range(10):
            if (i + j) % 2 == 0:
                cprint.cprint(f"{i}{j}", "red", "on_white")
            else:
                cprint.cprint(f"{i}{j}", "green", "on_white")
        for i in range(10):
            cprint.cprint(f"{i}", "yellow", "on_white")
draw_cake()